---
- name: Run automatic APT maintenance
  hosts: update_automatic
  become: true
  gather_facts: true
  serial: 1
  order: inventory
  tasks:
    - name: Run guarded automatic maintenance
      block:
        - name: Require a Debian-family system
          ansible.builtin.assert:
            that:
              - ansible_facts.os_family == "Debian"
            fail_msg: Automatic APT maintenance requires a Debian-family system.

        - name: Require maintenance notification credentials
          ansible.builtin.assert:
            that:
              - lookup('ansible.builtin.env', 'MAINTENANCE_NTFY_URL', default='') | length > 0
              - lookup('ansible.builtin.env', 'MAINTENANCE_NTFY_TOKEN', default='') | length > 0
            fail_msg: Maintenance notification environment variables are required.
          delegate_to: localhost
          become: false
          no_log: true

        - name: Read failed systemd units before maintenance
          ansible.builtin.command:
            cmd: systemctl --failed --no-legend --plain --no-pager
          register: automatic_failed_units_before_maintenance
          changed_when: false

        - name: Select failed systemd unit names before maintenance
          ansible.builtin.set_fact:
            automatic_failed_unit_names_before_maintenance: >-
              {{ automatic_failed_units_before_maintenance.stdout_lines
                 | default([])
                 | select('match', '^\s*\S+\s+\S+\s+failed\s+failed(?:\s|$)')
                 | map('regex_replace', '^\s*(\S+).*', '\1')
                 | unique
                 | sort
                 | list }}

        - name: Refresh APT cache when stale
          ansible.builtin.apt:
            update_cache: true
            cache_valid_time: 3600
            lock_timeout: 300
          register: automatic_apt_cache

        - name: Install distribution APT upgrade
          ansible.builtin.apt:
            upgrade: dist
            force_apt_get: true
            allow_downgrade: false
            allow_unauthenticated: false
            allow_change_held_packages: false
            dpkg_options: "force-confdef,force-confold"
            lock_timeout: 300
          register: automatic_apt_upgrade

        - name: Remove obsolete packages
          ansible.builtin.apt:
            autoremove: true
            purge: true
            force_apt_get: true
            lock_timeout: 300
          register: automatic_apt_autoremove

        - name: Remove obsolete package cache entries
          ansible.builtin.apt:
            autoclean: true
            lock_timeout: 300

        - name: Clean the APT package cache
          ansible.builtin.apt:
            clean: true
            lock_timeout: 300

        - name: Reload systemd manager configuration
          ansible.builtin.systemd_service:
            daemon_reload: true

        - name: Check whether a reboot is required
          ansible.builtin.stat:
            path: /var/run/reboot-required
          register: automatic_reboot_required

        - name: Read failed systemd units before orphan cleanup
          ansible.builtin.command:
            cmd: systemctl --failed --no-legend --plain --no-pager
          register: automatic_failed_units_before_cleanup
          changed_when: false
          failed_when: false

        - name: Select orphaned failed systemd units
          ansible.builtin.set_fact:
            automatic_orphaned_failed_units: >-
              {{ automatic_failed_units_before_cleanup.stdout_lines
                 | default([])
                 | select('match', '^\s*\S+\s+not-found\s+failed\s+failed(?:\s|$)')
                 | map('regex_replace', '^\s*(\S+).*', '\1')
                 | list }}

        - name: Reset each orphaned failed systemd unit
          ansible.builtin.command:
            argv:
              - systemctl
              - reset-failed
              - "{{ item }}"
          loop: "{{ automatic_orphaned_failed_units | default([]) }}"
          loop_control:
            label: orphaned failed unit

        - name: Read failed systemd units after orphan cleanup
          ansible.builtin.command:
            cmd: systemctl --failed --no-legend --plain --no-pager
          register: automatic_failed_units_after_cleanup
          changed_when: false
          failed_when: false

        - name: Require a successful post-maintenance systemd check
          ansible.builtin.assert:
            that:
              - automatic_failed_units_after_cleanup.rc | default(1) == 0
            fail_msg: Automatic maintenance could not read failed systemd units.

        - name: Select failed systemd unit names after maintenance
          ansible.builtin.set_fact:
            automatic_failed_unit_names_after_maintenance: >-
              {{ automatic_failed_units_after_cleanup.stdout_lines
                 | default([])
                 | select('match', '^\s*\S+\s+\S+\s+failed\s+failed(?:\s|$)')
                 | map('regex_replace', '^\s*(\S+).*', '\1')
                 | unique
                 | sort
                 | list }}

        - name: Classify failed systemd units after maintenance
          ansible.builtin.set_fact:
            automatic_new_failed_units: >-
              {{ automatic_failed_unit_names_after_maintenance
                 | difference(automatic_failed_unit_names_before_maintenance)
                 | sort
                 | list }}
            automatic_preexisting_failed_units: >-
              {{ automatic_failed_unit_names_after_maintenance
                 | intersect(automatic_failed_unit_names_before_maintenance)
                 | sort
                 | list }}

        - name: Report pre-existing failed systemd units
          ansible.builtin.debug:
            msg: >-
              Automatic maintenance did not introduce these failed systemd units,
              but they remain failed: {{ automatic_preexisting_failed_units | join(', ') }}
          when: automatic_preexisting_failed_units | length > 0

        - name: Require no new failed systemd units
          ansible.builtin.assert:
            that:
              - automatic_new_failed_units | length == 0
            fail_msg: >-
              Automatic maintenance introduced failed systemd units:
              {{ automatic_new_failed_units | join(', ') }}

        - name: Notify that a reboot is required
          ansible.builtin.uri:
            url: >-
              {{ lookup('ansible.builtin.env', 'MAINTENANCE_NTFY_URL', default='') }}
            method: POST
            headers:
              Authorization: >-
                Bearer {{ lookup('ansible.builtin.env', 'MAINTENANCE_NTFY_TOKEN', default='') }}
              Title: "Reboot required: {{ inventory_hostname }}"
              Priority: default
              Tags: warning
            body: >-
              Automatic maintenance completed on {{ inventory_hostname }} and the host
              requires a reboot.
            status_code: 200
          delegate_to: localhost
          become: false
          no_log: true
          when: automatic_reboot_required.stat.exists | default(false)

        - name: Report successful automatic maintenance
          ansible.builtin.debug:
            msg:
              inventory_hostname: "{{ inventory_hostname }}"
              cache_refreshed: "{{ automatic_apt_cache.changed | default(false) }}"
              upgrade_performed: "{{ automatic_apt_upgrade.changed | default(false) }}"
              autoremove_performed: >-
                {{ automatic_apt_autoremove.changed | default(false) }}
              reboot_required: >-
                {{ automatic_reboot_required.stat.exists | default(false) }}
              failed_systemd_units: >-
                {{ automatic_failed_unit_names_after_maintenance | length }}
              preexisting_failed_systemd_units: >-
                {{ automatic_preexisting_failed_units | length }}
              new_failed_systemd_units: >-
                {{ automatic_new_failed_units | length }}
              orphaned_failed_units_reset: >-
                {{ automatic_orphaned_failed_units | default([]) | length }}

      rescue:
        - name: Attempt automatic maintenance failure notification
          ansible.builtin.uri:
            url: >-
              {{ lookup('ansible.builtin.env', 'MAINTENANCE_NTFY_URL', default='') }}
            method: POST
            headers:
              Authorization: >-
                Bearer {{ lookup('ansible.builtin.env', 'MAINTENANCE_NTFY_TOKEN', default='') }}
              Title: "Maintenance failed: {{ inventory_hostname }}"
              Priority: high
              Tags: rotating_light
            body: "Automatic APT maintenance failed on {{ inventory_hostname }}."
            status_code: 200
          delegate_to: localhost
          become: false
          no_log: true
          failed_when: false

        - name: Fail automatic maintenance explicitly
          ansible.builtin.fail:
            msg: Automatic APT maintenance failed.
